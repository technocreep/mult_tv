services:
  transmission:
    image: lscr.io/linuxserver/transmission:latest
    container_name: transmission
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Moscow
      - USER=admin
      - PASS=admin1234
    volumes:
      - ./config:/config
      - ./downloads:/downloads
      - ./watch:/watch
    ports:
      - 9091:9091
      - 51413:51413
      - 51413:51413/udp
    restart: unless-stopped

  web_tv:
    build: .
    container_name: mult_tv_app
    ports:
      - 80:8000
    volumes:
      - ./downloads:/downloads
      - ./app:/app/data
    restart: unless-stopped
    depends_on:
      - transmission